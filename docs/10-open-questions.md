# 10 — Open questions

**Where to look:** what is still open first, then what was resolved and
when. Every answered question names where the answer came from, so a claim can
be traced back to the measurement that produced it.

Resolve these as they come up. When one is settled, move the answer into the
relevant doc and note it in `SESSION_LOG.md`.

## Open — read these first

**Nothing blocks work as of 2026-09-01.** Both hardware-access blockers are
dead, the instrument runs end to end, and real optical sweeps exist. What is
open is either physics waiting on hardware that has not arrived, or a mechanism
that is understood well enough to work around but not well enough to explain.
The full brief is `08-the-bench.md`; the current state is the HANDOFF
block at the top of `SESSION_LOG.md`.

| # | Question | Blocks |
|---|---|---|
| ~~Q28~~ | ~~The Ethernet link to the board is dead~~ — **RESOLVED 2026-08-28 by moving to a new control PC.** 1 Gbps, ping 1 ms, SCPI and key-based SSH both working. The root cause was never identified and the evidence pointed at the old PC's port; if it recurs, put the board on a switch rather than a direct cable | nothing |
| ~~Q27~~ | ~~The laser has never answered~~ — **ANSWERED 2026-08-28: USB is a hardware fault inside the instrument; LAN works.** The `CM_PROB_FAILED_INSTALL` driver state was a red herring. See the Q27 row below and `TSL775_HANDOFF.md` | nothing |
| **Q30** | **LARGELY ANSWERED 2026-09-03 BY THE MEASUREMENT ITSELF: a peak at the expected phase-matching wavelength is something the artefact cannot make.** With the crystal in and the APD on IN1, demodulating at 2*f1 gave a clear peak at ~1559 nm. The artefact has **the same wavelength shape as f1** -- that is what was measured with no crystal -- so its wavelength dependence is the broad transmission envelope of the path, and a narrow peak where phase matching is predicted is not something it can produce. **What is still missing is the POWER-SCALING control** (slope 1 for the artefact, slope 2 for SHG), which is cheap and would convert the argument into a number, and **confirmation of the detector model (Q38)**, which decides whether the confound could reach the detector at all. Original entry follows. **How is real SHG to be separated from the AOM's own second harmonic?** MEASURED 2026-08-28 with no crystal in the path: demodulating at 2*f1 gives **24 mV against 180 mV at f1 -- 13.3%, -17.5 dB -- and the SAME wavelength shape.** The shape is the confirmation, not a coincidence: an AOM diffracts as sin^2 of its drive and the drive is depth-1 AM, so the light is already distorted before it reaches anything, and that harmonic rides the same light through the same fibre onto the same detector. A DIFFERENT shape would have meant something else was happening. **Any SHG measurement must clear 13.3% before it has said anything.** Two separations: (1) **POWER SCALING** -- the AOM artefact is linear optics and goes as P^1 while SHG goes as P^2, so on log-log against laser power the background has slope 1 and SHG slope 2. This works with the crystal left IN, which crystal-in/out does not, and the laser's -5 to +13 dBm range gives an 18 dB lever arm. Measure the slope-1 baseline before the crystal arrives. (2) **The detector** -- the harmonic is carried on 1550 nm light, and SHG of 1550 is 775 nm, which the InGaAs PDA05CF2 cannot see at all (800-1700 nm). A silicon detector behind a filter blocking the fundamental never receives the confound. Raised and measured 2026-08-28. |
| ~~Q29~~ | **FIXED 2026-08-28: `reduce_sweep` now places the wavelength axis on the MEASURED edge times** (`use_edge_times=True`), falling back to a uniform grid only when the record does not hold one edge per logged row. Lost pulses are interpolated by ORDINAL, so a gap costs one estimated row rather than the measured axis. Re-reducing the real capture `data/sweep_001.npz` both ways, the two axes differ by up to **13.68 pm = 0.684 of a logged step** (rms 2.81 pm) on a 20 pm step -- that difference is the error the uniform grid was carrying. Original finding follows. **The laser's sweep speed ripples by +/-11%, so the trigger train is NOT uniform in time. MEASURED 2026-08-28.** The trigger is periodic in WAVELENGTH (Q24), so constant time steps require constant sweep speed, and the speed is not constant: gap sd 5.87 us on a 200 us step, 2061 of 5000 gaps more than 5 us off the median, peak-to-peak 43.8 us = 21.9%. The ripple is periodic with a period of **0.41 nm (~20.4 triggers, 4.1 ms)**. Integrated, it puts the edges up to **157.7 us = 0.79 of a step** away from a uniform grid, which is **15.8 pm of wavelength misassignment** if a uniform step is assumed -- against a laser whose own log is linear to 0.4 pm. **`reduce_sweep` currently assumes a uniform step** (span over N-1), so it is carrying that error today. **The fix costs nothing: use the measured edge times, which are already captured.** Physical cause not identified; a 0.41 nm period is suggestive of an etalon or of the grating drive. |
| ~~Q11b~~ | **ANSWERED 2026-08-28: YES.** The linear sweep gives 130 mV at 915.527 kHz with the drive on, nanovolts with OUT1 disarmed, and 15 mV with the laser dropped 9 dB -- a measured 8.67x against a predicted 7.94x, agreeing to 0.38 dB. The amplitude tracks OPTICAL POWER while the RF drive is unchanged, so it is light and not pickup. Original question follows. **Does the photodetector respond to light at all?** *(Power question SETTLED 2026-08-28: keep the laser under 1 mW -- below the ~0.96 mW saturation and so well below damage. Enforced as `--max-dbm`.)* It has only ever been observed with the shutter closed, where a healthy detector and a disconnected one look identical | the first real measurement |
| ~~Q22~~ | ~~Santec command set~~ — **ANSWERED 2026-08-14 from both manuals.** See `05-instruments.md`. The driver can now be written |
| ~~Q11~~ | ~~Photodetector output amplitude and impedance~~ — **ANSWERED 2026-08-14 from the manual and confirmed on the bench.** PDA05CF2, AC-coupled on LV, 289 mV swing with the beam on and no clipping. See the Q11 row below | nothing |
| ~~Q12~~ | ~~Safe drive levels~~ — **ANSWERED 2026-08-17: no attenuator.** See the Q12 row below, and do not reopen | nothing |
| **Q17** | Phase 2 success criteria | knowing when Phase 2 is *done*. Not when to start. The results it was waiting on are now in |
| **Q31** | **Why is the generator's frequency error so much larger than its resolution implies?** The output is `mod_cycles x play_rate`, so a play-rate error is multiplied by `mod_cycles` — that part is arithmetic and certain, and it is why 915 kHz planned as 12 cycles beat against a reference built from the same numbers while 1 MHz at 1 cycle did not. **The magnitude does not fit.** A 32-bit DDS accumulator at 250 MS/s realises 76250 Hz to within 0.0015 Hz, which is 0.018 Hz after x12; the observed beat was ~0.69 Hz, nearly 40x that. So the play rate is realised more coarsely than "quantised to 1 Hz" suggests, or something else is in the loop. **Worked around** by planning the fewest modulation cycles the carrier tolerance allows (2026-09-01), which is right regardless. Two cheap checks that would settle it, board only: read `SOUR1:FREQ:FIX?` back after setting it — the bench currently reads nothing back from the ASG — and measure the phase slope of a long loopback capture, which gives the actual generated frequency to sub-millihertz. Raised 2026-09-01 | nothing today; would matter for any plan needing many modulation cycles |
| **Q32** | **Why does writing `:WAV` stop the laser sweeping?** Driving the TSL-775 to its sweep start wavelength by hand succeeded — 1600 to 1500 nm in 0.41 s, read back on target — and then `:WAV:SWE 1` produced no trigger train at all, twice, where the same sequence without the `:WAV` write sweeps and triggers normally. So an explicit wavelength write leaves the instrument in a state where the sweep does not run, even with every sweep setting still correct and the wavelength exactly where the sweep wants to begin. **Worked around** by letting the laser return to start on its own and waiting between Configure and Start. The change that provoked this was reverted (`61088d0`). Raised 2026-09-01 | a Start button that could guarantee the full range |
| **Q33** | **What exactly leaks the laser's LAN connection slots?** Measured 2026-09-01: port 10001 went from accepting to silently dropping SYNs after exactly two connect-and-close cycles, with nothing else on the network talking to the instrument, while every closed port on the same host kept answering RST normally. Port 5000 was already in that state. Only a power cycle recovers it — a front-panel LAN reset alone did not. The clean-FIN handling in `TcpTransport.close()` was written as a guess at this and is now ruled out as a complete fix. **Working rule: one connection, held for the whole session; never retry a failed connect, power cycle instead.** Raised 2026-09-01 | nothing, given the rule |
| **Q36** | **Is the output's peak-to-peak factor constant and linear?** Q23 was measured at one point only -- a commanded 0.200 V. **Command 0.400 V and read the scope again**: 140 mV RMS confirms a constant factor of 0.5. Until then the peak-to-peak finding is a single-point measurement. Raised 2026-09-03 | nothing today; it would change the drive-level arithmetic and the `constants.py` comment |
| **Q37** | **Two labelling defects on the bench, both known.** The capture log line reports `swing()`, which is PEAK-TO-PEAK, next to trace numbers that are ZERO-TO-PEAK, with no units named on either. And `run_demodulate` / `run_map` both hardcode `gain="LV"` rather than reading the front end that is actually set, so an HV capture would be converted 20x wrong. Raised 2026-09-03 | correct absolute volts on an HV channel |
| **Q38** | **What is actually on IN1, and what are its specifications?** The detector was changed to a Thorlabs APD410-series unit and the docs still describe the PDA05CF2 in several places. **The model suffix decides the spectral range** -- APD410A is InGaAs (~900-1700 nm), APD410A2 is silicon (~200-1000 nm) -- and that is load-bearing for the SHG plan, whose whole point is a detector that cannot see the fundamental. Read the label. Raised 2026-09-04 | the SHG detector choice, and every absolute optical figure |

## Was blocking Phase 1 — all resolved

| # | Question | Where it got answered |
|---|---|---|
| Q2 | Are the SCPI commands in `hardware.py` correct for that version? | **YES, all of them, H1.5 complete.** `setup_am_generator` needed rewriting rather than respelling — its model of the generator was wrong. The `ACQ:AXI:*` path is verified through `acquire_deep_fast`, which H6.2–H6.5 and H7.1 exercised for 20+ full-length captures. |
| Q3b | Is the ASG's 16384-entry table size settable over SCPI? If so, exact 80/5/6 MHz returns and the frequency limitation in `SESSION_LOG.md` disappears. | **Never probed, and no longer blocking.** The grid plan was adopted instead and works: carrier 80.001831 MHz, difference 991.821 kHz. Worth a look only if exact round frequencies ever matter. |
| Q4 | Does `SOUR:TRig:INT` start both channels synchronously? | **YES — H2.4 passed 2026-08-12.** Both channels generate simultaneously with carrier magnitudes within 0.6%. Note this is about them *starting* together; their relative phase is Q6, which is a different question and failed. |
| Q5 | Is Deep Memory Generation available on this OS? | **NO — answered 2026-08-14.** All nine candidate SCPI spellings return zero bytes, and sending a 32768-entry table **closes the SCPI connection outright**. The generator's unique-waveform ceiling is 16384 samples = 65.536 µs, permanently. H5.4's fallback applies. |

## Affects measurement quality

| # | Question | Notes |
|---|---|---|
| Q6 | Is the OUT1/OUT2 relative carrier phase repeatable? | **CLOSED 2026-08-14.** The offset is random at start (71–82° across restarts) but **constant within a run** — measured drift 0.0024 Hz, excursion 0.053° over 24 ms, against a 2250 Hz bandwidth. A constant offset does not affect R, and the deliverable is amplitude only (Kevin, 2026-08-12). The residual drift risk that survived that ruling is now measured and dead. Do not reopen unless phase becomes a deliverable. |
| Q7 | Are IN1 and IN2 sample-aligned? | **YES — answered 2026-08-14. Aligned to 0.004 ns = 0.0005 samples**, repeatable to 0.002 ns over 1–20 MHz. No correction needed; channel skew biases nothing. Measured properly, with OUT1 split through a BNC tee into both inputs, which is the only wiring that separates input skew from output skew and from the ASG's random start phase. See H4.3. |
| Q8 | What is the real noise floor at 1 MHz? | **Answered 2026-08-12 — see Resolved.** |
| Q9 | Does the DUT response roll off at 1 MHz? | Physics, not measurable in loopback. Lower difference frequencies are available — see `03-frequency-plan.md`. |

## Needs a human decision

| # | Question |
|---|---|
| Q11 | **ANSWERED 2026-08-14 from the Thorlabs manual.** The detector is a **PDA05CF2**: InGaAs, 800–1700 nm, **150 MHz bandwidth**, 0–10 V into Hi-Z (10⁴ V/A), 0–5 V into 50 Ω, 2 mV rms output noise, NEP 1.26×10⁻¹¹ W/√Hz, saturating around 0.96 mW optical. Full entry in `05-instruments.md`. **Two consequences.** (1) **U4 is closed** — 150 MHz means 991.821 kHz is deep in the flat region, and "does the detector roll off at 1 MHz" was a risk to the whole premise. (2) **The detector will dominate the noise, not the ADC**: ~11 µV against the board's 3.57 µV, so **SNR 10 needs ~120 µV rather than 36 µV**. See `06-results.md`. The 0–10 V unipolar output cannot go straight into the ±1 V range, so **AC-couple IN1** — the ±20 V range would put the ADC back in charge at 45 µV. |
| Q12 | **ANSWERED 2026-08-14 — and the answer is NO ATTENUATOR; leave the drive where Kevin tuned it.** *(Revised twice: 20 dB → 10 dB → 6 dB. Kevin measured 800 mVpp at 80 MHz on a **1 MΩ** scope; the amplifier's 50 Ω halves that to −4 dBm, and the board also loses **14 dB** at 80 MHz — which answers U1 for free. The current no-attenuator setup is 5 dB BELOW compression, not into it as earlier notes said.)* Amplifier **ZHL-1-2W+** (32 dB gain typ, P1dB +33 dBm, **absolute max input +10 dBm**), AOM **1550AOM-1** (2.5 W nominal RF at 80 MHz, 0.5 W optical handling). **Without an attenuator the board at full scale sits exactly on the amplifier's damage rating** — ±1 V into 50 Ω is +10 dBm and the rating is +10 dBm. 6 dB is the least attenuation meeting all three constraints: damage impossible (board would need +30 dBm), 11 dB below compression (U2 — the governing one, since amplifier intermodulation lands exactly on the measurement frequency), and first-order optical power 0.39 mW against the detector's 0.96 mW ceiling. **At 15 dB the optics saturate the detector before the electronics compress.** Full working in `05-instruments.md`. |
| Q13 | **DECIDED 2026-08-14 (Kevin): NO averaging.** The experiment sweeps detuning 1 across ~11–13 discrete wavelength settings of detuning 2, so each sweep is its own measurement rather than one of many to be combined. Phase therefore does not need to stay coherent between sweeps, and buffer management stays simple. |
| Q18 | **ANSWERED 2026-08-14 (Kevin): the Santec triggers at set TIME steps, and only the FIRST edge is used** — to synchronise the laser and the Red Pitaya. So IN2 carries a periodic pulse train, but the alignment depends on one edge, not on recovering the train intact. **The missed-edge question is therefore closed for alignment purposes**, and with it the case for the memory move. But see Q21 — "the first edge" has to mean the same edge on both sides, and that is not automatic. |
| Q19 | **ANSWERED 2026-08-14 (Kevin): closely synchronised already, and an external timebase can be attached if needed.** Worth knowing that it need not be taken on trust: because the trigger fires at **fixed time steps** (Q18), the recorded train is a direct measurement of the two clocks' ratio — fit a line through the recorded edge times and compare the slope against the laser's nominal step. **This converts U11 from an assumption into a per-sweep measurement, using data already captured, for free.** Recommended even with an external timebase fitted, as a check that it is actually working. |
| Q20 | **ANSWERED 2026-08-14 (Kevin), and his description was right.** The laser logs **wavelength values with the time axis IMPLICIT**: `:READout:DATa?` returns a bare array and `:READout:POINts?` its length, so `wavelength[i]` belongs to trigger pulse `i`. With the trigger stepping in time, that **is** wavelength against relative time from the first trigger — the times are reconstructed as `first_edge + i × step` rather than transmitted. *(An earlier revision of this entry called Kevin's answer wrong. It was not: he described the semantics, the manual describes the wire format, and both are true.)* **The distinction that does matter:** a reconstructed time axis depends on two things a transmitted one would not — that the trigger really is stepping in time (**Q24**, whose encoding the two manuals define oppositely) and that there is one log point per pulse (**Q26**, which no manual states). If either is false the times are wrong and nothing in the data would show it, which is why `check_alignment` compares the record's pulse count against `:READout:POINts?`. |
| Q23 | **MOSTLY ANSWERED 2026-09-03 with a scope: the 1817.7 counts/V ADC constant is CORRECT, and the 0.882 factor lives on the OUTPUT side.** A commanded 0.200 V on OUT1 read 70 mV RMS = 99 mV amplitude on the scope, while the bench's lock-in trace read 100 mV -- 1% agreement. That is the half that mattered: the photodetector drives the input directly, so every absolute figure in `06-results.md` stands rather than being 12.7% high. **A second finding came with it: `SOUR<n>:VOLT X` commands X volts PEAK-TO-PEAK**, so a commanded 0.2 V is a 0.1 V amplitude and every drive level has been 6 dB more conservative than the attenuator arithmetic assumed. **Still outstanding: a 0.400 V scope reading**, to confirm the factor is a constant 0.5 and linear. Original question follows. Is the 1817.7 counts/V constant a property of the ADC, or of the DAC? A commanded 0.5 V returns 902.8 counts, implying 1816.9 counts/V -- matching the inherited 1817.7 to 0.04%, from an unrelated measurement. But loopback measures DAC x cable x ADC as one number and cannot say where the 0.882 factor lives, because the only instrument available to measure the generator is that same ADC. Raised 2026-08-14. |
| Q22 | **ANSWERED 2026-08-14 from both manuals** (TSL-775 v1.0 supplied by Kevin; TSL-770 from santec.com). Command set, data format, interfaces and delimiter are all recorded in `05-instruments.md` under "The Santec lasers". **The driver is unblocked.** Two traps found in the process: the two models define `:TRIGger:OUTPut:SETTing` with **inverted** encodings, and the Santec's delimiter is bare CR where the Red Pitaya's is CRLF. |
| **Q27** | **Why does the laser not answer over USB? A concrete lead appeared 2026-08-26.** On the control PC the laser's virtual COM port node reports `CM_PROB_FAILED_INSTALL` (`FTDIBUS\VID_2428+PID_0116+2601S967A\0000` — the TSL-775 exactly), and no COM ports were present on the machine at all. **The VCP driver never installed correctly**, which the earlier "host side is eliminated" conclusion did not account for. Whether this dates from August or arose since is unknown. **The cheapest test is a clean driver install on different hardware.** Still eliminated: cable, 6 baud rates, 3 terminators, flow control, and the D2XX path. Still untried and cheap: line settings other than 8N1 (7E1, 8E1), a power cycle to clear a stuck REMOTE state, Santec's own software, and LAN. Raised 2026-08-20, re-opened 2026-08-26 — **and ANSWERED 2026-08-28: USB is a HARDWARE FAULT INSIDE THE INSTRUMENT, and the driver state was a red herring.** The FT232H enumerates and the PC-to-chip half is healthy, but the instrument never replies over any USB path, while the identical commands with the identical `\r` delimiter work perfectly over LAN. **Use LAN — `10.101.0.197:5000`, bare CR, one held connection.** Do not spend more time on USB; it is a warranty item. See `TSL775_HANDOFF.md`. |
| **Q28** | **RESOLVED 2026-08-28 — the new control PC links at 1 Gbps and the board answers.** The cause was never identified, so this is a symptom cleared rather than a fault understood. Original entry follows. **Why did the Ethernet link to the board die, and which end failed?** Raised 2026-08-26. From the control PC's event log: linking at 1 Gbps through 14 Aug; on 25 Aug it linked at 1 Gbps then fell to 100 Mbps twice (gigabit needs four pairs, 100 Mbps needs two), and **has not linked at any speed since 14:20 that day** — including with a new cable. Separately the NIC logged **107 resets in one day** against a background of 1–6, in a loop every 4–10 seconds. **The board is exonerated**: its LEDs show it boots and runs, which also clears the SD-card and device-tree worries. `Get-NetAdapterPowerManagement` on the PC returns "a device attached to the system is not functioning". **The leading suspect is the control PC's port.** Decisive test: cross-check each end against a third device. Recommended fix either way — put the board on a switch rather than a direct cable, which also ends the link-local address churn. **This blocks every hardware step.** |
| Q26 | **DEAD as of 2026-08-25 — the design stopped depending on it.** The question was whether the laser logs exactly one wavelength per trigger pulse, which no manual states. It mattered only while the time step came from the trigger INTERVAL. `pipeline.reduce_sweep` derives the step from the trigger train's **span** over (N − 1) logged points, which never counts pulses — so a laser logging one point per five pulses gives the same answer. Comparing `:READout:POINts?` against the recorded pulse count is still worth doing at P2 as a sanity check (`check_alignment` does it), but nothing rests on the outcome. See `07-pipeline.md`. |
| Q25 | **ANSWERED 2026-08-14 by measurement, and the answer is "nothing to worry about".** The high-pass corner is **17.0 Hz**, single-pole — three points fit one pole to 2%. At 991.821 kHz that is **1.3×10⁻⁹ dB** of attenuation, and AC coupling does not cost 0.1 dB until below ~78 Hz. **The noise floor is also unchanged**: demodulated σ was 0.00601 counts DC against 0.00586 AC, a 2.5% difference on 392 points whose own uncertainty is 3.6%. **So every figure in `06-results.md` carries over to AC coupling.** Measured as an AC/DC ratio, so Q23 cannot affect it. |
| Q24 | **ANSWERED 2026-08-28 on the bench: on the TSL-775, 0 = periodic in WAVELENGTH.** The readback alone cannot settle it — a 0 is a 0 under either manual — but the sweep runs at 0 and produces sub-picometre-linear points at exactly 0.02 nm spacing, which is only consistent with that encoding. **The TSL-775 manual is right; the TSL-770's table is the erroneous one.** It matters less than feared: at constant sweep speed, uniform-in-wavelength is also uniform-in-time, which is what `reduce_sweep` needs. Original question follows. **Which way round is `:TRIGger:OUTPut:SETTing`?** The TSL-775 manual (p100) says 0 = periodic in wavelength, 1 = periodic in time. The TSL-770 manual (p99) says **the exact opposite**. One of them is a documentation error, or the models genuinely differ. **Either way the driver must set it and read it back rather than assume**, and the failure is silent: the wrong mode still produces a trigger train, just periodic in the wrong variable, which would make the wavelength spacing wrong without anything looking broken. Resolve on the bench at P1. Raised 2026-08-14. |
| Q21 | **NEW, and the one that can silently ruin a sweep: is the Red Pitaya's "first trigger" the same edge as the laser's?** Both sides define t = 0 as the first trigger, but they define it independently. If the acquisition arms late and latches the *second* pulse, every wavelength is offset by exactly one time step and **nothing in the trace looks wrong** — the shape is identical, just mislabelled. Mitigations, both cheap: arm the capture before the sweep starts and use pre-roll (H6.4 proved this works), and cross-check the number of pulses in the record against the length of the laser's table. Raised 2026-08-14. |
| Q14 | **ANSWERED 2026-08-25 (Kevin): yes — and SUPERSEDED 2026-08-31 by `scripts/bench.py`,** a panel bench with independent operations that compose into a sweep, which is what Kevin asked for after using the tabbed one. `bench_gui.py` is kept and still works; it is the only path with a Simulate mode needing no hardware. Both go through `scripts/_bench_ops.py`, so there is no second implementation of any instrument operation. Original answer follows. `scripts/bench_gui.py` -- a Tkinter front end for driving the implemented features by hand: board connect and front end, the two-tone drive, deep capture, demodulation, a trace view, CSV/npz output, and the laser link. Tkinter because it is stdlib and the bench box has nothing installed; the plot is a Tk canvas because matplotlib is an optional extra and is not present. It has a **simulate** path so the whole capture -> demodulate -> view -> CSV chain can be exercised with nothing connected. Safety is part of the design, not decoration: outputs off on close, an OUTPUTS OFF button visible from every tab, a confirm dialog naming channel and frequencies before any output is enabled, laser writes gated behind a checkbox that starts off, and every command and reply logged. No control restarts the SCPI server. **The unattended-operation question stays deferred** -- this is an attended tool. |
| Q15 | **DECIDED 2026-08-14 (Kevin): CSV** for the transfer-function output. Human-readable, opens anywhere, and the per-sweep volume is small (5000 rows). Recommend keeping the raw capture in `.npz` alongside it — CSV of 32 M samples would be absurd, and re-deriving a trace from raw is the only way to revisit a demodulation choice after the fact. |

## Resolved

| # | Question | Answer |
|---|---|---|
| Q1 | Red Pitaya OS version | **2.00, build 37** (Ubuntu 22.04.4, kernel 5.15.0-xilinx, commit `a0457d3aa`). In `/opt/redpitaya/version.txt`; `/etc/redpitaya_version` does not exist on this image. |
| Q3 | **RE-MEASURED 2026-08-28 AND THE OLD ANSWER WAS WRONG.** A short buffer DOES produce output: the board treats what you write as the whole table, so the frequency is `cycles x play_rate x 16384/N` (measured 65.54 = 16384/250). SOUR:FREQ:FIX also accepts 1 MHz and 5 MHz, so there is no 15258 Hz ceiling and the fs/16384 grid is only the default. A 16384-entry table with 1 modulation cycle and 80 carrier cycles played at 1 MHz gives exactly 80 MHz AM'd at exactly 1 MHz. The only real limit is that the play rate is quantised to 1 Hz. Original answer follows. Does the generator accept a 250-sample arbitrary buffer? | **No — and the question was based on a wrong model.** The ASG always traverses a fixed 16384-entry table; `SOUR:FREQ:FIX` sets the traversal rate, not a per-sample clock. A 50-sample buffer produces no output at all. |
| Q3a | Move the frequency plan onto the 15258.789 Hz grid? | **Yes, decided by Kevin 2026-08-12 and implemented.** Carrier 80.001831 MHz, f1 5.004883 MHz, f2 5.996704 MHz, difference **991.821 kHz**. The limitation this imposes is recorded in full in `SESSION_LOG.md` at his request — in particular, **do not hardcode 1e6 as the lock-in frequency.** |
| Q8 | What is the real noise floor at the lock-in frequency? | **51.7 nV/√Hz on IN1** at 991.821 kHz, giving **σ = 3.57 µV per quadrature** at the operating bandwidth (decimation 2, DC, ±1 V range, outputs off, loopback cables fitted — Kevin accepted the cable-on configuration rather than a 50 Ω terminator on 2026-08-12, as it is the wiring Phase 1 runs in). 3.57 ppm of full scale. **Revised twice from an original 2.96 µV, which was ~15% optimistic** — re-measured independently by two mutually agreeing routes, and separately with 50 Ω terminators, which showed the loopback **cable adds ~50%** to the floor (terminated: 34.6 nV/√Hz, 2.39 µV). **A signal of ≥36 µV amplitude at the ADC gives SNR 10 on every trace point with no sweep averaging**; 24 µV is the unreachable terminated best case, and the real cable from a photodetector will be worse than ours, not better. On the ±20 V range, σ = 45 µV (2.3 ppm of range). Two caveats that matter: the conversion from density to σ uses a noise gain of **4232.7 Hz, which is 1.88× the nominal 2250 Hz bandwidth**, not equal to it; and a switching-supply harmonic sits 17.9 kHz off the lock-in frequency, rejected by >200 dB today but only **1.77% of switcher drift** away from landing on it — where it would read as a **32 µV steady amplitude, 11× the noise floor and indistinguishable from a healthy real signal.** Full detail and the five things this does not cover are in `SESSION_LOG.md` 2026-08-12. |
| Q10 | τ = 30 µs or 71 µs? | **71 µs at 5000 points**, decided by Kevin 2026-08-12. Forcing 30 µs at 5000 points would fold 2805 Hz of noise onto every trace for 3.7 dB worse SNR, buying resolution a 5000-point grid cannot represent. The aliasing-free way to reach ~30 µs is 12500 points (τ = 28.3 µs); considered, not taken. Recorded in `01-overview.md`. |

## Deferred

| # | Question |
|---|---|
| Q16 | Does anything need continuous analog output of the demodulated signal? Only this would justify revisiting FPGA — see ADR-0001. |
| Q17 | Phase 2 success criteria. Deliberately not set until Phase 1 results are in. Those results are in; it now waits on the crystal, since "what counts as a detection" depends on what the DUT actually gives. |
| Q34 | **Unattended operation.** Nothing runs unattended and nothing needs to yet. Needs a real answer before any long run with the amplifiers live. Deferred at Kevin's request since 2026-08-25. |
| Q35 | **Two laser drivers with different surfaces.** `scripts/tsl775.py` (`TSL775`, write/query only) is what the bench uses; `src/rp_lockin/santec.py` (`SantecTSL`, a much richer surface) is what `pipeline.py` assumes. Writing ops against the wrong one shipped a bug on 2026-09-01 that the suite could not catch, because the test fake had been modelled on the wrong one too. Not urgent, but they should converge. |
